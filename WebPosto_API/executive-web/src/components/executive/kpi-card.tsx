"use client";
import { motion } from "motion/react";
import { Area, AreaChart, ResponsiveContainer } from "recharts";
import { ArrowDownRight, ArrowUpRight } from "lucide-react";
import { Card, CardContent, CardHeader } from "@/components/ui/card";
import { brl } from "@/lib/utils";
interface Props { label: string; value: number; trend?: number | null; data: number[]; kind?: "currency" | "ratio" }
export function KpiCard({ label, value, trend = null, data, kind = "currency" }: Props) { const positive = trend === null || trend >= 0; return <motion.div initial={{ opacity: 0, y: 8 }} animate={{ opacity: 1, y: 0 }}><Card className="overflow-hidden"><CardHeader className="pb-1"><span className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">{label}</span>{trend !== null && <span className={positive ? "text-success" : "text-critical"}>{positive ? <ArrowUpRight size={17}/> : <ArrowDownRight size={17}/>}</span>}</CardHeader><CardContent><strong className="font-mono text-2xl tracking-tight">{kind === "ratio" ? value.toFixed(2) : brl(value)}</strong><div className="mt-3 flex items-end justify-between"><span className={trend === null ? "text-xs text-muted-foreground" : positive ? "text-xs font-medium text-success" : "text-xs font-medium text-critical"}>{trend === null ? "Sem comparativo" : `${positive ? "+" : ""}${trend.toFixed(1)}%`}</span><div className="h-9 w-24">{data.length > 1 && <ResponsiveContainer><AreaChart data={data.map((v, i) => ({ i, v }))}><defs><linearGradient id={`spark-${label}`} x1="0" x2="0" y1="0" y2="1"><stop offset="0" stopColor={positive ? "#10b981" : "#ef4444"} stopOpacity={.35}/><stop offset="1" stopColor={positive ? "#10b981" : "#ef4444"} stopOpacity={0}/></linearGradient></defs><Area type="monotone" dataKey="v" stroke={positive ? "#10b981" : "#ef4444"} fill={`url(#spark-${label})`} strokeWidth={2}/></AreaChart></ResponsiveContainer>}</div></div></CardContent></Card></motion.div>; }

"use client";
import { Area, AreaChart, CartesianGrid, Cell, Legend, Line, LineChart, Pie, PieChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import type { FlowPoint } from "@/lib/treasury";
import { brl } from "@/lib/utils";
const tooltip = { formatter: (value: unknown) => brl(Number(value || 0)), contentStyle: { borderRadius: 10, border: "1px solid #33415522" } };
export function CashFlowChart({ data }: { data: FlowPoint[] }) { return <ResponsiveContainer width="100%" height={310}><AreaChart data={data}><defs><linearGradient id="inflow" x1="0" x2="0" y1="0" y2="1"><stop offset="0" stopColor="#10b981" stopOpacity={.3}/><stop offset="1" stopColor="#10b981" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="#94a3b822"/><XAxis dataKey="period" tickLine={false} axisLine={false} fontSize={11}/><YAxis tickFormatter={v => `${Math.round(v/1000)}k`} tickLine={false} axisLine={false} fontSize={11}/><Tooltip {...tooltip}/><Legend/><Area type="monotone" dataKey="inflow" name="Entradas" stroke="#10b981" fill="url(#inflow)" strokeWidth={2}/><Area type="monotone" dataKey="outflow" name="Saídas" stroke="#ef4444" fill="transparent" strokeWidth={2}/></AreaChart></ResponsiveContainer>; }
export function CompositionChart({ data }: { data: { name: string; value: number }[] }) { const colors=["#3b82f6","#10b981","#f59e0b","#8b5cf6"]; return <ResponsiveContainer width="100%" height={310}><PieChart><Pie data={data} dataKey="value" nameKey="name" innerRadius={72} outerRadius={105} paddingAngle={3}>{data.map((item,i)=><Cell key={item.name} fill={colors[i%colors.length]}/>)}</Pie><Tooltip {...tooltip}/><Legend verticalAlign="bottom"/></PieChart></ResponsiveContainer>; }
export function ForecastChart({ data }: { data: FlowPoint[] }) { return <ResponsiveContainer width="100%" height={280}><LineChart data={data}><CartesianGrid vertical={false} stroke="#94a3b822"/><XAxis dataKey="period" tickLine={false} axisLine={false} fontSize={11}/><YAxis tickFormatter={v => `${Math.round(v/1000)}k`} tickLine={false} axisLine={false} fontSize={11}/><Tooltip {...tooltip}/><Legend/><Line type="monotone" dataKey="best" name="Melhor cenário" stroke="#10b981" strokeWidth={2} dot={false}/><Line type="monotone" dataKey="expected" name="Esperado" stroke="#3b82f6" strokeWidth={3} dot={false}/><Line type="monotone" dataKey="worst" name="Pior cenário" stroke="#ef4444" strokeDasharray="5 5" strokeWidth={2} dot={false}/></LineChart></ResponsiveContainer>; }

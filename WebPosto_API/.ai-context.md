# 🎯 Project Context Snapshot

## 💻 Tech Stack Atual

### Backend (Python / FastAPI) — `WebPosto_API/`
- **Python** 3.10+ (Poetry) / 3.12+ (subprojeto gateway)
- **FastAPI** ^0.104 (Poetry) · ^0.115 (requirements.txt pip)
- **Uvicorn** ^0.24 / ^0.30
- **Pydantic v2** + pydantic-settings
- **HTTP client:** httpx, tenacity, requests
- **Cache:** Redis / Valkey (`redis` ^5.0)
- **DB:** SQLAlchemy 2, Alembic, psycopg2, aiosqlite
- **Logging:** structlog, python-json-logger
- **Jobs (opcional):** Celery
- **Testes:** pytest, pytest-asyncio, pytest-cov, responses, testcontainers

### Frontend — monorepo `Api_WebPosto/` (pai)
- **Next.js 16 (Turbopack):** `frontend/cockpit/` — aplicação standalone com Shadcn/ui (SaaS Premium Dark / Omie Style) rodando na porta 3000
- **React 18.2** + **Vite 5** — `frontend/react-vite/` (API Explorer, axios)
- **UI em produção anterior:** dashboards HTML estáticos + JS vanilla (`dashboard_vendas.html`, `static/dashboard_logos.html`, `theme/cockpit.js`, `theme/executive.js`)
- **Reflex pages:** `webposto/webposto.py`, `src/presentation/frontend/pages/`
- **dashboard.jsx** isolado em `WebPosto_API/src/frontend/`

### Infra / Integração
- **Docker Compose** + Valkey
- **Cliente WebPosto** (`src/webposto/`) — ~154 endpoints Quality Automação
- **Subprojeto:** `logos-webposto-gateway/` (FastAPI porta 8050, CRUD financeiro)

### Entry point recomendado
```bash
# Terminal 1 — API (porta 8050)
cd WebPosto_API
python src/presentation/app.py

# Terminal 2 — Frontend Cockpit Next.js 16
cd frontend/cockpit
npm run dev
```

---

## 📌 Onde Paramos (Status Atual)

**Branch:** `fix/pydantic-validators`

### ✅ Funcional e validado com dados reais (POSTO VIP)
- **Gateway unificado** — `src/presentation/app.py` (proxy, CORS, GZip, cache Valkey, UI)
- **Integração WebPosto real** — chave API ativa; leitura de EMPRESAS, ABASTECIMENTO, VENDA, CAIXA, PRODUTO
- **Abastecimento ontem (21/05/2026)** — 918 registros, **5.626 L** bate com relatório do posto (Gasolina 3.194,717 L · Diesel 207,475 L · Gas. Aditivada 354,067 L · Etanol Aditivado 1.869,518 L)
- **Paginação correta ABASTECIMENTO** — cursor `ultimoCodigo` (`src/infrastructure/clients/webposto_pagination.py`)
- **Health check** — `/health`, `/api/health/webposto` (valida via `EMPRESAS`, não `FILIAL`)
- **Adelaide KPIs** — `/api/v1/adelaide/metrics`, `/overview`, jobs async
- **Catálogo produtos & CRUD (Next.js 16)** — `/produtos` migrado com sucesso de `theme/produtos.js` e `produtos_crud.html` para componente nativo `produtos-live.tsx` com formulário CRUD interativo, filtros refinados (múltiplos grupos, subgrupos 1/2/3, tipo do item, situação ativa/inativa e paginação)
- **Hydration Mismatch resolvido** — relógio baseado em Recife encapsulado no componente customizado `RecifeClock` para carregar exclusivamente client-side, eliminando divergências do SSR.
- **Preços e Custos** — cruzamento dinâmico em tempo real de `PRODUTO` e `PRODUTO_EMPRESA` para obter preços de venda Tabela A e custos fiscais reais.

### ⚠️ Incompleto / com ressalvas
- **POST produto** — implementado no gateway e frontend, mas API Quality retorna **404** (contrato não habilitado)
- **Auth** — mock (`admin@company.com` / `password`); JWT parcial
- **`/metrics/stream`** — stub; `/metrics/executive` pode 500 (redis_adapter)
- **Testes** — 3 suites falham na coleta (Settings vs variáveis extras no `.env`); cobertura ~12%
- **Múltiplos entry points** — `presentation/app.py`, `main.py`, `main_minimal.py`, `logos-webposto-gateway`, monorepo pai — sobreposição operacional
- **Git** — centenas de alterações locais não commitadas

### Rotas principais do gateway (`src/presentation/app.py`)
| Área | Rotas |
|------|-------|
| Health | `GET /health`, `/api/health/webposto`, `/api/config` |
| Proxy WebPosto | `POST /api/webposto/proxy`, `GET /api/v1/proxy/{path}` |
| Adelaide | `GET /api/v1/adelaide/metrics`, `/overview`; `POST .../metrics/async`; `GET /api/v1/jobs/{id}` |
| Produtos | `GET/POST /api/v1/webposto/produtos`, `PUT .../{id}`, `GET .../grupos-produto` |
| Executive | `GET /api/executive/kpis`, `GET /api/v1/dashboard/filtros` |
| UI | `GET /`, `/dashboard`, `/produtos`, `/app/vendas` |
| Cache | `GET /api/cache/stats` |

---

## 🚀 Próximos Passos Imediatos (Para a próxima sessão)

1. **Testes** — ajustar as 3 suítes que quebram pela configuração de ambiente `Settings`
2. **Consolidar entry point** — documentar e usar apenas `src/presentation/app.py`
3. **Estabilizar Valkey Cache** — monitorar o hit ratio e gerenciar falhas de conexão de maneira resiliente
4. **Git hygiene** — criar commits limpos de todo o cockpit Next.js e alterações da API
5. **Quality Automação** — solicitar a habilitação de POST para produtos e ajustes de estoque para a conta da filial VIP

---

## ⚠️ Regras de Ouro do Projeto
- P99 target sempre < 80ms (Queries otimizadas).
- Padrão visual SaaS Premium Dark (Shadcn/ui).
- Não alterar arquivos fora do escopo da tarefa.
